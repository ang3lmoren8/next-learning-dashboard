import Layout from '@/app/dashboard/layout'
import DefaultNotFound from '@/components/DefaultNotFound/DefaultNotFound'

const NotFound = () => {
  return (
    <Layout>
      <DefaultNotFound title='Not found Pokemon' />
    </Layout>
  )
}

export default NotFound
